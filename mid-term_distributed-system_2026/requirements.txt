# Runtime
fastapi==0.136.0          # ASGI web framework (includes Starlette + Pydantic v2)
uvicorn[standard]==0.40.0 # ASGI server (pulls in uvloop + httptools for performance)
pydantic==2.11.3          # Data validation & event schema (bundled with FastAPI, pinned explicitly)

# Testing
pytest==8.3.5             # Test runner
pytest-asyncio==1.3.0     # Async test support (asyncio mode for FastAPI + asyncio.Queue tests)
httpx==0.28.1             # Async HTTP client used with FastAPI's TestClient / ASGITransport8